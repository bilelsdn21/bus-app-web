# Copy to .env.local for local override, or set in Vercel (production).
# Local dev talks to the local backend by default (http://127.0.0.1:8000).
# In Vercel, set this to your Render backend URL:
# VITE_API_URL=https://bus-app-backend.onrender.com
